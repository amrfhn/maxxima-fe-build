import "../scss/index.scss"
import "bootstrap";

import 'core-js';
import 'regenerator-runtime/runtime';

$(function () {
    console.log('Document ready!')
})

if (module.hot) {
    module.hot.accept()
}